import hikeList from './hikes.js';

function getHikeData() {
    return JSON.parse(hikeList);
}

function getSingleHikeByName(name) {

     

    return hikeInfo;
}
 


export {getHikeData, getSingleHikeByName};